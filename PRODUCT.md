# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

- `[추정 — 저장소 기반]` Google·Microsoft 또는 IMAP 메일 계정을 가진 개인 사용자가 여러 계정의 받은편지함을 한곳에서 확인하고 정리한다.
- `[추정 — 저장소 기반]` 사용자는 광고·결제·스미싱 의심 메일을 빠르게 구분하고, 중요한 메일만 남기며, 필요할 때 직접 삭제하려고 한다.
- `[확인 필요]` 팀 요금제와 공통 정리 정책이 실제 출시 대상인지, 그리고 팀 관리자·구성원 역할이 필요한지는 정해지지 않았다.

## Product Purpose

싹메일(운영: 심플소프트)은 사용자가 연결한 Gmail, Outlook, IMAP 메일을 조회하고 분류·정리하도록 돕는 서비스다. 사용자는 받은편지함을 훑고, 메일의 상세 내용을 확인하고, 추천을 참고해 유지·휴지통 이동·영구 삭제를 선택하거나 자동정리함으로 옮긴 메일을 되돌릴 수 있다. 성공 기준과 정량 목표는 저장소에 정의되어 있지 않다(`[확인 필요]`).

## Positioning

`[현재 구현에서 도출]` provider별 인증/API/IMAP 차이를 감추고 한 화면에서 메일을 정리하며, 개인화 AI 추천은 선택 동의와 민감정보 마스킹을 전제로 제공한다. 추천은 최종 삭제를 대신하지 않고, 자동정리는 삭제가 아닌 별도 메일함 이동으로 제한해 사용자의 통제와 복구 가능성을 남긴다. 이 조합이 외부 경쟁 서비스와 비교한 공식 차별점인지 여부는 `[확인 필요]`다.

## Operating Context

- 하나의 저장소에 `apps/web`(데스크톱 우선 반응형 웹)과 `apps/mobile`(모바일 우선 웹)이 있으며, 두 앱이 공통 UI·메일·인증 패키지를 사용한다. 네이티브 iOS/Android 앱은 확인되지 않았다.
- 기본 흐름은 로그인 또는 메일 계정 연결 → 메일 조회 → 받은편지함/싹메일 자동정리함 전환 → 카테고리 필터·상세 열람 → 추천 피드백·휴지통·영구 삭제다.
- Google OAuth는 Gmail API 권한을, Microsoft OAuth는 Microsoft Graph 메일 권한을 메일 연결 시점에 추가 요청한다. IMAP은 메일 주소·서버·포트·앱 비밀번호로 연결을 확인한다.
- 선택형 AI 추천과 자동정리 설정은 메일 화면의 동의·설정 영역에서 관리한다. AI 동의 철회와 학습 데이터 삭제 흐름도 제공한다.
- 서비스 문서는 Cloudflare Workers, D1, Vectorize, Workers AI 및 Google/Microsoft/사용자 지정 IMAP 서버를 운영 경계로 설명한다.
- 로컬 `pnpm dev`는 Node에서 실행되며 IMAP raw TCP를 제공하지 않으므로 IMAP 흐름은 `apps/web` 또는 `apps/mobile`의 Cloudflare preview 경로에서 검증해야 한다.

## Capabilities and Constraints

- 지원 연결 방식: Google Gmail, Microsoft Outlook, 네이버·다음·카카오 등 IMAP 서버 및 직접 지정한 IMAP 서버. Microsoft 연결 버튼은 자격 증명이 설정된 경우에만 노출된다.
- 메일 기능: 페이지네이션 목록, 상세 열람, 발신자·제목·날짜 기반 분류, 광고·결제 완료·스미싱 의심 카테고리, 이미지 표시 제어, 휴지통 이동, 복구 가능한 자동정리함 이동, 되돌릴 수 없는 영구 삭제.
- 개인화: 사용자의 유지·비선호·삭제 피드백을 바탕으로 선호도와 추천을 만들고, 자동정리 신뢰도 임계값을 조정한다. AI 추천은 선택 동의가 없으면 제공하지 않는다.
- 개인정보 경계: 메일 원문은 처리 중에만 사용하고 별도 데이터베이스에 저장하지 않는다고 문서화되어 있다. AI 처리 전 이메일·전화번호·주민등록번호 형태·인증번호·계좌/카드번호·비밀 URL을 마스킹한다.
- 인증정보 경계: IMAP 앱 비밀번호와 서버 정보는 암호화된 NextAuth 세션 쿠키에만 보관하고 데이터베이스·로그에는 저장하지 않는다. 세션 만료 또는 로그아웃 시 폐기한다.
- 사용자가 AI 동의와 국외 이전 동의를 거부해도 메일 조회와 수동 삭제는 이용할 수 있어야 한다. 자동 추천은 메일을 자동 영구 삭제하지 않는다.
- 영구 삭제는 되돌릴 수 없으므로 실행 전 확인이 필요하다. 외부 메일 제공자의 인증·정책·장애에 따라 연결이 중단될 수 있다.
- 가격 상태에 불일치가 있다: 이용약관은 현재 모든 기능이 무료라고 설명하지만, `packages/ui/src/pricing.tsx`와 `packages/billing`에는 무료·플러스·팀 요금제와 PortOne 결제 흐름이 있다(`[확인 필요]`).
- 법률 문서는 공개 전 변호사 또는 개인정보 전문가의 최종 검토가 필요하다고 명시한다. 이 기록은 법률 준수 판단을 대신하지 않는다.

## Brand Commitments

- 제품명: `싹메일`; 웹 도메인: `ssakmail.com`; 운영 주체 표기: `심플소프트`.
- 현재 화면과 메타데이터는 한국어를 기본 언어로 사용하며, 짧고 정리 중심인 카피를 사용한다. 예: `전할 말만 남기고 싹 정리했습니다.`, `필요한 소식을 깔끔하게 전하는 싹메일`(`[현재 구현에서 관찰]`).
- 개인정보처리방침과 이용약관, 운영 문의 주소(`simplecode@simplesoft.me`)가 제품 표면에 포함되어 있다.
- 시각적 세계, 색상, 서체, 로고 자산을 미래 작업의 고정 규칙으로 삼을지는 `[확인 필요]`다.

## Evidence on Hand

- 제품 설명과 운영 제약: `README.md`.
- 주요 표면: `apps/web/app/page.tsx`, `apps/mobile/app/page.tsx`, 각 앱의 pricing/privacy/terms 라우트.
- 공통 메일 UI와 사용자 흐름: `packages/ui/src/mail.tsx`, `packages/ui/src/imap-connect.tsx`, `packages/ui/src/pricing.tsx`.
- 인증·메일·추천·결제 구현: `packages/auth`, `packages/gmail`, `packages/mail`, `packages/preference`, `packages/billing`.
- 개인정보·약관 카피: `packages/ui/src/privacy.tsx`, `packages/ui/src/terms.tsx`.
- 회귀 단위 테스트: `packages/*/src/*.test.ts(x)`에 메일·인증·추천·결제 계약 테스트가 있다.
- 사용자 인터뷰, 사용량 지표, 고객 사례, 검증된 추천사, 별도 브랜드 자산은 저장소에서 찾지 못했다. 이를 새로 만들어내지 않는다.

## Product Principles

1. 사용자가 메일의 최종 유지·삭제 결정을 가진다.
2. 자동화는 삭제보다 되돌릴 수 있는 정리와 설명 가능한 추천을 우선한다.
3. 필요한 데이터만 동의와 마스킹을 거쳐 처리하고, 원문·인증정보의 보유를 최소화한다.
4. provider가 달라도 메일을 읽고 정리하는 핵심 흐름은 일관되어야 한다.
5. 연결 실패·권한 부족·추천 불가 상태를 숨기지 않고 명시적으로 보여준다.

## Accessibility & Inclusion

- 현재 구현에는 문서 언어(`lang="ko"`), 키보드용 `:focus-visible`, `aria-live`, 입력 레이블과 삭제 확인 대화상자 등 기본 접근성 신호가 있다.
- `[확인 필요]` WCAG 준수 수준, 스크린 리더 지원 범위, 색상 대비 기준, 저대역폭·고령 사용자 등 우선 접근성 요구는 정해지지 않았다.
- `[확인 필요]` 현재 제품 언어는 한국어 중심이며 다국어 지원 계획은 확인되지 않았다.
